name: flutter_engineer_codecheck
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:

  flutter_riverpod:

  freezed_annotation: 
  freezed: 

  http: 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

  build_runner:
  json_serializable:

flutter:

  uses-material-design: true